# 03 Library Page

Goal: build the main cinematic library surface.

- Replace the author sidebar and card wall with a dense media layout.
- Add compact featured header, top search, left filter drawer, author rails, and category rails.
- Sync filters with URL query params.
- Rails reflect active backend filters.
- Add empty, loading, and error states.

Acceptance:

- Refresh preserves search/filter state.
- Browser back/forward works for filter changes.
- Library is dense and media-first.
- Mobile uses compact layout and bottom-sheet-style panels.
